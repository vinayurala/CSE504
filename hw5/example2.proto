void main()
{
	int a, b, c;
	b = 2;
	c = 3;
	a = 1;
	//a = ++b * c-- + 5;
	//a = -b + c;
	b = 1;
	a = 4;
	c = a - 5 - b;
	while (b < 2 && a > 3) do
	{
		c = b % 2;
		b++;
		a--;
	} 
	print (a+b);
	return;
}